/**
 * URL pubblico del sito (notifiche, link assoluti). Nessuno slash finale.
 */
const PUBLIC_SITE_URL = 'https://www.disciplinafit.it';

export function getPublicSiteUrl(): string {
  return PUBLIC_SITE_URL;
}
